# Application Environment
# 'production' enables security features and hides detailed error messages
NODE_ENV=production

# Server Port
# The port number the server will listen on
PORT=5000

# MongoDB Connection String
# URL for connecting to MongoDB database
MONGODB_URI=mongodb://localhost:27017/blog-app

# JWT Secret Key
# Used for signing and verifying JSON Web Tokens
# IMPORTANT: Change this to a long, random string in production
JWT_SECRET=your-super-secret-jwt-key-here

# Frontend URL
# Used for CORS configuration to allow requests from frontend
# Change this in production to your actual frontend domain
FRONTEND_URL=http://localhost:3000 